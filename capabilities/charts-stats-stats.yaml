naftiko: 1.0.0-alpha2
info:
  label: Blockchain.com Charts, Stats & Market Data API — Stats
  description: |
    Blockchain.com Charts, Stats & Market Data API — Stats. 1 operations. Lead operation: Blockchain.com Get Bitcoin Network Statistics. Self-contained Naftiko capability covering one Blockchain.com business surface.
  tags:
    - Blockchain.com
    - Charts, Stats & Market Data API
    - Stats
  created: '2026-05-30'
  modified: '2026-05-30'
capability:
  consumes:
    - type: http
      namespace: charts-stats-stats
      baseUri: https://api.blockchain.info
      description: Blockchain.com Charts, Stats & Market Data API — Stats business capability. Self-contained, no shared references.
      resources:
        - name: stats
          path: /stats
          operations:
            - name: getStats
              method: GET
              description: Blockchain.com Get Bitcoin Network Statistics
              inputParameters: []
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: $.
  exposes:
    - type: rest
      namespace: charts-stats-stats-rest
      port: 8080
      description: REST adapter for Blockchain.com Charts, Stats & Market Data API — Stats. One Spectral-compliant resource per consumed operation, prefixed with /v1.
      resources:
        - path: /v1/stats
          name: stats
          description: REST surface for stats.
          operations:
            - method: GET
              name: getStats
              description: Blockchain.com Get Bitcoin Network Statistics
              call: charts-stats-stats.getStats
              with: {}
              outputParameters:
                - type: object
                  mapping: $.
    - type: mcp
      namespace: charts-stats-stats-mcp
      port: 9090
      transport: http
      description: MCP adapter for Blockchain.com Charts, Stats & Market Data API — Stats. One tool per consumed operation, routed inline through this capability's consumes block.
      tools:
        - name: get-bitcoin-network-statistics
          description: Blockchain.com Get Bitcoin Network Statistics
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: charts-stats-stats.getStats
          with: {}
          outputParameters:
            - type: object
              mapping: $.
